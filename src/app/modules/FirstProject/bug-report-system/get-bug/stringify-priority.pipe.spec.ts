import { StringifyPriorityPipe } from './stringify-priority.pipe';

describe('StringifyPriorityPipe', () => {
  it('create an instance', () => {
    const pipe = new StringifyPriorityPipe();
    expect(pipe).toBeTruthy();
  });
});
